node_modules
dist
dist-ssr
*.local
.DS_Store

# secrets / env — per-component env files (Backend/.env, Frontend/.env are matched by `.env`).
# `.env.ai` needs its own line (different name). `*.example` templates stay tracked.
.env
.env.ai

# claude code local config / tooling (kept local, not published)
# NOTE: CLAUDE.md is intentionally TRACKED — it's the project's operating contract, so a
# fresh clone of this repo carries its own rules (mirrors ../PiKaOs-docs/CLAUDE.md; keep both
# in sync when a rule changes). See CLAUDE.md §6.
.claude/
.mcp.json
.playwright-mcp/

# local debug / test artifacts (screenshots, scratch scripts)
ratch-pair-diff.png
Backend/smoke_*.py

# python / backend
__pycache__/
*.pyc
*.pyo
.venv/
.pytest_cache/
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
