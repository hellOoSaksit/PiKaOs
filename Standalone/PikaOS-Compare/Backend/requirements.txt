# Website Compare (standalone) — minimal deps. Compare is stateless, so NO database /
# redis / minio / auth libraries are needed (no sqlalchemy, asyncpg, alembic, redis, minio,
# pyjwt, argon2). Just the web framework, the HTTP client, and pydantic. Pins match the
# parent PiKaOs repo as of 2026-06-16.
fastapi==0.137.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
httpx==0.28.1
